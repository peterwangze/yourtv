# 电视直播 APP 痛点调研与优化方案（聚源TV）

> 本文档沉淀"调研 → 根因 → 优化 → 验证"全链路结论，作为本仓库体验优化工作的依据与交付物。
> 适用版本：v2.5.0（versionCode 250）

## 一、调研背景

真实用户反馈的三大问题：

1. 电视安装后**首次进入极慢**，卡在"解析源"近一分钟，随后画面一闪而过、**比例不对**，播放不起来；
2. **解析源卡住时直接退出**，再次进入直接黑屏把电视卡死；
3. 要求**全界面中文**、**所有源在国内网络可用**。

## 二、行业普遍痛点与解法对照

| 痛点 | 行业常见根因 | 主流解法 | 本 APP 处理 |
|---|---|---|---|
| 首次启动/换源慢，卡"解析源" | 弱网无超时、源站串行下载、UI 线程同步解析、失效镜像 | 并发下载 + 超时保护 + 后台解析 + 本地缓存 | 45s 总超时、源内容 24h 缓存、频道解析结果文件缓存、首启降载（只探测前 200 台） |
| 重进黑屏卡电视 | 竞态崩溃、异常处理自杀进程、播放器释放策略不当、大缓存拖慢启动 | 就绪保护、不自杀、暂停不释放、缓存移出 SP | updateConfig context 就绪保护、异常处理器不自杀、onStop 只暂停、大字符串改文件缓存 |
| 画面比例不对 | 全屏拉伸（stretch）、缺少按原始比例留黑边 | RESIZE_MODE_FIT / 等比留边 | 全屏/非全屏均 FIT，4:3/21:9 源不变形 |
| 切台慢/闪屏 | 无预加载、缓冲策略差 | 备用播放器预准备下一频道 | standbyPlayer 后台预加载下一频道、低缓冲策略（500ms 出画面）、连接预热 |
| 无限重试刷屏卡顿 | 列表未就绪时循环请求焦点 | 重试次数上限 + 附着/判空保护 | ListAdapter 5 次上限、MenuFragment 10 次上限 |
| 国内网络不可用 | 默认源被墙（jsDelivr 等）、GitHub 直连不稳 | 国内镜像 + 多镜像轮换 | 默认源重排 6 个国内可用源、GitHub 镜像优先 + 原地址兜底 |
| 界面非中文 | 默认语言英文、分组名取源数据英文 | 默认中文字符串 + 分类映射 | values/strings.xml 中文化（175 key）、30 个英文分组映射 |
| 解析提示刷屏 | 多源并行导入重复弹 Toast | 会话级去重 | AtomicBoolean 只弹一次"解析源" |

## 三、本 APP 根因定位（代码级）

### 3.1 首启慢 / 解析源卡（问题 1 前段）

- `Utils.getUrls()`：镜像列表含已失效域名（ghproxy.com、github.moeyy.cn、ghproxy.net），命中即长时间等待；
- `DownGithubPrivate.downloadFile()`：无总超时，单个请求可无限挂起，与用户"卡近一分钟"现象吻合；
- 修复：镜像按国内可用性重排（moeyy.xyz、ghfast.top、gh-proxy 等），公共 GitHub URL 镜像优先 + 原地址兜底；下载增加 `totalTimeoutMs = 45_000` 总超时。

### 3.2 重进黑屏卡死（问题 2）

- 竞态：重进时 `ready()` 早于 `init()`；
- 异常处理器直接 `killProcess` 自杀；
- `onStop` 释放播放器导致 HOME 往返重建；
- 修复：`updateConfig` context 就绪保护、异常处理器不再自杀、`onStop` 只暂停不释放、大缓存字符串迁出 SharedPreferences。

### 3.3 画面比例不对（问题 1 中段）

- `PlayerFragment.updatePlayerViewLayout()` 强制全屏拉伸；
- 修复：统一 `RESIZE_MODE_FIT` + 居中留黑边；PiP 按视频实际宽高比（`player.videoSize`）计算，21:9/4:3 均有兜底。

### 3.4 无限重试刷屏（体验劣化）

- `ListAdapter.toPosition()`：itemCount==0 或未附着时无限 `post` 重试，实测 20 次/秒；
- `MenuFragment.onVisible()`：同样无限重试；
- 修复：分别加 5 次/10 次上限 + `isAttachedToWindow` + `itemCount>0` 判空。

### 3.5 国内网络可用性（问题 3）

- 默认源含被墙的 jsDelivr；GitHub raw 直连在国内不稳定；
- 修复：`SP.DEFAULT_SOURCES` 重排为 6 个国内直连/官方域名源；GitHub 拉取走镜像链；EPG 默认 `live.fanmingming.cn` 国内域名优先。

## 四、优化落地清单（v2.4.6 → v2.5.0）

| 版本 | 内容 | 关键文件 |
|---|---|---|
| v2.4.6 | 图标透明、源精简、修复首启卡解析源、修复重进黑屏、缓存迁出 SP | MainViewModel.kt、YourTVExceptionHandler.kt |
| v2.4.7 | 解析结果缓存秒出、1s 启动/切台、线路健康探测、快速换线 | MainViewModel.kt、PlayerFragment.kt、LineHealth.kt、SourceQuality.kt |
| v2.4.8/v2.4.9 | 无缝切台（备用播放器）、连接泄漏修复、EPG 重试冷却、换线循环保护 | PlayerFragment.kt、DownGithubPrivate.kt |
| v2.5.0 | 全界面中文 + 分组中文化、国内源重排 + GitHub 镜像、45s 下载超时、无限重试修复、EPG/刷新源入口、解析 Toast 去重 | strings.xml、MainViewModel.kt、SP.kt、Utils.kt、DownGithubPrivate.kt、ListAdapter.kt、MenuFragment.kt、SettingFragment.kt |

## 五、验证证据（模拟器 emulator-5554）

- 冷启动：安装 v2.5.0 release 后启动无崩溃（PID 正常，无 AndroidRuntime 错误）；
- 设置面板：标题"聚源TV v2.5.0"，按钮/开关全中文（远程配置、换视频源、更新节目单、刷新直播源等）；
- 频道分组：英文分类自动转中文（娱乐/宗教/未分类/新闻/少儿/综合/商业/体育/电影/教育/文化/纪录片）；
- 源下载：zbds.top（iptv4.txt 121,838B）、iptv-org cn（60,430B）、cn_all（204,046B）、zho（87,022B）实测拉取成功并写入缓存；
- 切台：方向键切换正常，信息卡更新，无焦点重试刷屏日志（修复前 20 次/秒）；
- EPG：国内域名与 GitHub 镜像在当前网络逐一失败后优雅降级，不崩溃；
- 单线路频道：提示"此频道只有一个直播源"，不卡死。

## 六、遗留风险与后续建议

1. **镜像可用性随时间变化**：当前网络下 moeyy.xyz/ghfast.top/gh-proxy 部分返回 403/404。建议在真实国内宽带上验证，必要时扩充镜像候选或改为运行时探测选优；
2. **EPG 源稳定性**：fanmingming e.xml 在本环境不可达，需真机确认；可增加 EPG 多源健康度标记；
3. **后续可做**：源健康度主动探测并展示（当前仅播放失败时动态标记）、切台预测预加载扩展到"常用频道"、分组映射支持用户自定义；
4. **发布流程**：版本号统一为 versionCode=250（version.json 与 build.gradle 一致），避免旧编码 20409 恒判"有新版本"。
 
 ## 七、v2.6.0 频道分类 + 多源聚合 + 弹窗治理
 
 ### 7.1 弹窗刷屏（用户反馈：观看中不断弹出解析弹窗）
 
 - 根因：多源导入/解析/失败路径多处直接 `Toast.show()`，无节流，弱网下同文案反复弹；
 - 修复一：`YourTVApplication.toast()` 全局节流（ConcurrentHashMap 记录，同文案 8 秒最多 1 条）；
 - 修复二：`importFromUrl` 新增 `silent` 参数，自动导入/聚合阶段源失败静默跳过（不弹"下载在线直播源失败"），仅手动切源/刷新保留提示；
 - 实测：连续观看 60 秒仅 1 条 Toast 日志；全新安装 10 秒内无失败弹窗。
 
 ### 7.2 频道分类（用户反馈：分类不合理，参考电视家）
 
 - 新增 `ChannelClassifier`：央视（CCTV/CGTN 台号规范化 cctv1…cctv17）/ 港澳台（台湾/香港/澳门品牌库+英文兜底）/ 地方卫视 / 地方频道（33 省 + 近 400 城市 → 省份映射，含港澳台）/ 海外频道（约 80 国家/地区关键词库）/ 其他；
 - 侧栏分组：央视 → 卫视 → 省份（按行政区划排序）→ 国家 → 其他；频道列表固定排序，默认频道落在 CCTV1；
 - `mergeKey()` 跨源合并：CCTV 用台号（CCTV-1综合 与 CCTV1 合并）、卫视/地方/海外用规范名（去括号/清晰度后缀/标点）、其他叠加原分组；`canonicalAlias()` 英文→中文（Hunan TV→湖南卫视、TVB Jade→翡翠台 等），并新增 BRTV 北京卫视→北京卫视 别名（北京卫视 10→11 条线路）。
 
 ### 7.3 单源频道不可用（用户反馈：部分频道仅 1 个不可用源）
 
 - 多源聚合：首源导入成功后后台并发导入全部预置源（每源 50s 兜底超时），按"分类+规范名"合并去重，央视/主流卫视线路 10~30 条，按清晰度（URL 关键词+实测分辨率）降序，坏线由运行时健康探测/播放失败自动跳过；
 - 源列表修正：移除失效的 YueChan APTV.m3u（404，实际文件为组播 IPTV.m3u）与区域封锁的 wcb1969（GitHub 451）、fanmingming.com ipv4（404）；新增 best-fan cn_province/cn_cctv 专项源，默认 13 个源全部国内直连或 GitHub 镜像可加速；
 - 实测聚合：9~12 源 / 643~644 频道；CCTV1=20、CCTV4=23、CCTV5=29、CCTV1-17 全部 ≥13；主流卫视湖南 17/东方 20/浙江 13/广东 13/北京 11/天津 18/山东 18/四川 16 等 ≥10；江苏卫视 9 条为当前公开源生态的上限（跨 5 源真并集，无合并遗漏）。
 
 ### 7.4 附带修复（深度检视）
 
 - 激活源缓存缺失时不再自动 `switchSource(0)` 整体替换频道列表（避免聚合结果被内置源覆盖）；"已是当前源"不再误报"加载失败"（改用 aready_current_source 文案）；
 - 聚合静默导入（apply=false）不再并发写入 `active_source`（竞态会把激活源指向随机源，实测指向 APTV.txt），首源导入后才移动激活源；
 - 频道缓存写改为同步写，消除 WRONG_FINAL_BLOCK_LENGTH 半截文件。
 
 ### 7.5 v2.6.0 验证证据
 
 - 全新安装 release：3 秒"解析中"提示 → 约 23 秒 CCTV1 出画，无卡死、无失败弹窗刷屏；
 - 二次启动：缓存列表秒出直接起播；聚合结果不覆盖（643~644 频道保持）；
 - 菜单分组：我的收藏/央视/卫视/北京/上海/河北/吉林/黑龙江/江苏/浙江/安徽/福建/山东…（省份分类生效）；
 - 无崩溃：多次冷启动/切台/开菜单无 AndroidRuntime FATAL；
- 版本：versionCode=260 / versionName=2.6.0（build.gradle、根 version.json、app/version.json 一致）。

## 八、v2.7.0 三级分级列表 + 高质量源集成

### 8.1 三级分级频道列表（用户要求：央视/卫视/地方/海外/其他为一级）

- 一级分类固定为 央视 / 卫视 / 地方 / 海外 / 其他（另有 我的收藏/全部频道 两个特殊分组）；
- 央视、卫视两级直达频道（一级=央视，二级=频道本身）；
- 地方（二级=省份，含港澳台与"未分类"兜底）、海外（二级=国家/地区）、其他（二级=分类）为三级；
- 交互：右键/确认键下钻地区列表，左键/返回键逐级退回，菜单顶部路径提示实时显示"地方 › 河北"；
- 实现：`TVGroupModel` 新增导航状态（NavEntry + enterCategory/exitCategory/restoreNav），`GroupAdapter` 按导航条目渲染，`MenuFragment` 处理下钻/返回/路径，`ChannelClassifier.topCategoryOfGroup` 提供分组→一级分类映射；
- 修复：播放频道定位到真实分组（原逻辑停留在"全部频道"导致重开菜单导航错乱）；同一 BACK 事件被 Activity.onKey 与 OnBackPressedDispatcher 双分发导致"退出下钻即关菜单"，用一次性标志幂等处理；路径提示与焦点分组同步（焦点回调先于 setPosition 执行，需显式传分组索引）。

### 8.2 高质量源调研与集成（用户要求：咪咕4K源质量高，继续调研集成）

- 调研结论（实测）：zbds.top（750 台）、best-fan 系列（881 台）为原有主力；新发现并实测：
  - `jk2024988/TV2024 咪咕2.m3u`：62 台全咪咕直播（CCTV1-17/卫视/地方），仓库活跃（2026-07 更新，932 star）；
  - `hououinkami/AppleTV China_v4.m3u`：咪咕 aikan.miguvideo.com CCTV1-17 + APTV bestv 8M 卫视，每日维护（2026-08-07 更新）；
  - `zhmzjj310144/migu-sports 三源合并_央视体育地方综合源.m3u`：492 台，含浙江卫视4K（ali-xwl.cztv.com）、湖南卫视4K（mgtv）、卫视超清、地方台；
  - 淘汰项：tv.iill.top（JS 跳转页）、HerbertHe/Yiov/guoweiok 等仓库路径失效、migu_sports_public（148/261 为回放噪音）。
- 集成：预置源 13 → 16 个；解析时过滤咪咕 VOD 域名（gslbmgspvod/depository_eos）与 历年春晚/更新时间 垃圾分组；
- 排序：`SourceQuality` 新增咪咕 90 分/百视通 APTV 88 分档；4K/8K/超清改词边界匹配（修复"64k 音频、jiangsuhd"误判 4K）；聚合排序加入稳定源加权（1000000）；
- 分类质量：体育-今天05-10 每日轮换分组 → 体育；Animation;Kids → 动画；补齐 30+ 县市与品牌（中国蓝/快乐垂钓）映射；地方频道分组无省市 → 地方›未分类。

### 8.3 v2.7.0 验证证据（模拟器 emulator-5554）

- 单元测试：ChannelClassifier（分类/合并键/规范化/分组本地化）与 SourceQuality（评分/实测分辨率/标题辅助）共 11+4 项全绿；
- 聚合实测：14~16 源 / 947 频道（v2.6.0 为 643~644）；分类分布 央视53/卫视77/地方564/海外39/其他214；CCTV1=24、CCTV4=31、CCTV5=39、东方卫视=23、江苏卫视=12 线路；咪咕线路 235、4K/UHD 线路 23；
- 菜单导航实测：央视/卫视两级直达；地方›省份›频道、海外›国家›频道、其他›分类›频道 三级下钻与逐级返回通过；播放任丘综合（河北）后重开菜单自动恢复 地方›河北 并高亮当前频道；返回键先退出下钻、再按关闭菜单；
- 稳定性：多轮冷启动/切台/下钻/返回无崩溃、无 ANR；
- 版本：versionCode=270 / versionName=2.7.0（build.gradle、根 version.json、app/version.json 一致），release APK 已发布至 GitHub Releases。

## 九、v2.8.0 分级导航打磨 + 高质量源再升级

### 9.1 分级列表交互优化（用户要求：央视/卫视两级、地方/海外/其他三级）

- 结构核对：一级分类固定 央视/卫视/地方/海外/其他（另含 我的收藏/全部频道）；央视、卫视两级直达频道；地方（省份）/海外（国家）/其他（分类）三级下钻——与 v2.7.0 一致；
- 交互补齐（本轮发现并修复）：原实现里分组面板按确认键直接关闭菜单，导致"确认下钻"名不副实。现在确认键在 央视/卫视/收藏/全部 及下钻后的地区/分类上直接进入频道列表；三级分类确认键直接下钻地区/分类列表；返回键逐级退出（幂等）；
- 繁体兼容（本轮发现并修复）：iptv-org 等源含繁体写法（"北京衛視 (1080p) [Geo-blocked]"），此前被误分入 地方-北京。新增繁体→简体字表（衛視/有線/新聞/綜合 等 60+ 字），"北京衛視"与"北京卫视"跨源合并为同一频道（19 条线路）；展示名清理 "(1080p) [Geo-blocked]" 类原始后缀；
- 卡酷少儿（北京广播电视台）品牌映射 → 地方-北京。

### 9.2 高质量源调研与集成（用户要求：咪咕之外继续调研）

- 调研（2026-08 实测拉取比对 20+ 候选仓库）：
  - `hujingguang/ChinaIPTV cnTV1_ALL.m3u8`：405 台，含各省广电官方流（l.cztvcloud.com 66 条、hw-m-l.cztv.com 浙江 1080p、live.dxhmt.cn 电信 66 条）、CCTV-16 4K（移动 dbiptv.sn.chinamobile.com）、凤凰香港、咪咕 5 条；仓库有版本日志持续维护；
  - `vbskycn/iptv iptv4.m3u`：446 台，含东方卫视4K（bp-resource-dfl.bestv.cn）、苏州4K（kan0512）、CCTV4K（xinketongxun）、cctvcloud 46 条、浙江广电 22 条；
  - `vicjl/myIPTV TV-IPV4.m3u`：58 台，黑龙江移动 OTT（ottrrs.hl.chinamobile.com）CCTV1-17/CETV/卫视，运营商级线路稳定；
  - 淘汰项：akiralereal/iptv（71 台但 rtmp/代理源居多）、Meroser-IPTV（IPv6 组播为主）、imDazui（失效）、BurningC4（404）、best-fan status 变体（与已集成的非 status 列表重叠）；
- 集成：预置源 16 → 19 个；
- 点播/循环噪音过滤（新增 5 类）：影视资源站 ffzy、斗鱼/虎牙 7x24 转播循环 metshop.top、央视点播片段 newcntv.qcloudcdn.com / cntv.lxdns.com、快手视频文件 kwimgs.com、"XXXX年春晚"回放标题；聚合后噪音残留 0 条；
- 评分扩展：芒果TV（mgtv.com）88、浙江广电（cztv/cztvcloud）87、运营商 OTT（chinamobile/dxhmt/gmcc/mobaibox）85、苏州4K（kan0512，URL 含 4k 走 95 档）。

### 9.3 v2.8.0 验证证据（模拟器 emulator-5554，API 36）

- 单元测试：ChannelClassifier / ChannelMetadataParser / SourceQuality 共 25 项全绿（含新源真实行解析、点播 URI 过滤、繁体合并、评分档位）；
- 聚合实测：19 源 → 17 个成功 → 合并 1190 频道（过滤点播后较 v2.7.0 净增 243 台）；CCTV1=27、CCTV5=30、CCTV5+=14、CCTV16=17（含 CCTV-16 4K 移动源）、CCTV4K=3、湖南卫视=20（芒果 4K 排前）、东方卫视=21（bestv 4K 排前）、浙江卫视=16（cztv 4K 排前）、江苏卫视=21、广东卫视=22；
- 菜单导航实测（截图留档 tools/s*.png）：一级分类 5 项正确；确认键 央视→CCTV 列表直达；地方→北京→频道 三级；其他→体育→频道 三级；返回退出下钻；播放后重开菜单恢复到当前频道分组；"北京衛視 (1080p) [Geo-blocked]" 不再出现在 地方-北京；
- 稳定性：多轮冷启动（秒出缓存列表）/下钻/切台/播放失败换线无崩溃、无 ANR；点播噪音频道在列表与日志中均无残留；
- 版本：versionCode=280 / versionName=2.8.0（build.gradle、根 version.json、app/version.json 一致），release APK 发布至 GitHub Releases（peterwangze/yourtv）。

## 十、v2.9.0 频道质量全量治理（用户反馈六类问题的根因与修复）

### 10.1 用户反馈与根因对照

| 用户反馈 | 根因 | 修复 |
|---|---|---|
| 观看频道与侧边栏不一致（漂移） | applyChannelList 按"id=列表下标"匹配稳定源，聚合后 id 重排 → 匹配失败 → 侧边栏被重置到 CCTV1 而播放不变 | 当前频道按 分类+规范名 重新锚定；频道 id 改为 mergeKey 稳定哈希；setCurrent 不再把过期对象追加进分组 |
| 选 CCTV1 却跳到 CCTV2 / 频道持续往后跳 | ①旧构建首启"无稳定源"路径 + id 错位；②聚合源首行线路为过期/错位 URL（咪咕 kailu 过期、tsfile 代理错位） | ①锚定修复 + 稳定 id；②播放时后台探测全线路，坏线标记后自动跳过；广告/点播/广播域过滤 |
| 频道混入广告源 | ①stream.ads.ottera.tv（"韩国电影1/2"实为广告流）；②咪咕虚拟直播（抗战经典影片/新片放映厅）；③快手视频文件 yximgs；④WebView 兜底页（yangshipin CCTV-4K 实测播出电视购物） | 新增 广告网络域/广播电台/虚拟直播/快手视频/图文直播/昨日赛事 过滤；IPTV 与 WEBVIEW 同名频道不再合并线路 |
| 未分类/乱分类泛滥 | 繁体台名不匹配（萍鄉/東森/華視）；iptv-org 英文台名无映射（Jiangxi/Harbin/QTV）；缺县级台映射；"XX综合"与"XX综合频道"不合并 | 繁体转简进入分类链路；40+ 英文省市关键词；100+ 县级台映射；频道/电视台后缀归一 |
| 省级主流频道缺失 | 大量省级频道其实存在，但以"吉林都市/安徽经济生活"等无省名标题落入未分类 | 分类修复后全部归位到对应省份（吉林都市/安徽经济/青海经视/浙江钱江都市/广东影视/黑龙江都市等） |
| 部分频道显示有问题 | ①蜻蜓FM 广播电台冒充电视（黑屏）；②Activity 配置变更销毁期 NPE 崩溃；③WebView 线路混入 IPTV | 广播域过滤；releaseStandby/onDestroy 判空；Manifest 全面接管 configChanges；跨类型频道不合并 |

### 10.2 v2.9.0 验证证据（模拟器 emulator-5554，API 36）

- 单元测试：30 项全绿（新增繁体/英文台名归类、频道后缀合并、广告/广播/虚拟直播 URI 过滤、过期赛事标题过滤）；
- 聚合实测：19 源 → 1072 频道；未分类 115→3（仅剩 数码时代/新闻综合/都市剧场）；央视51/卫视49/台湾24/香港12/澳门6/河南131/浙江97/江苏72/吉林50/云南45/四川39/广东37/湖南36/山东35/甘肃34/黑龙江32；
- 漂移回归：选择 CCTV1 后持续观察 110s+，日志全程 CCTV1 无频道跳转；坏线自动跳过（nextVideo 从当前线跳到第 13 条健康线）；收藏（稳定 id -1060187977）跨重启保留且列表红心正确显示；
- 崩溃回归：多轮 force-stop/重启、聚合期间、配置变更路径均无 AndroidRuntime FATAL（修复了 Activity 销毁期 NPE）；
- 版本：versionCode=290 / versionName=2.9.0，release APK 发布至 GitHub Releases（peterwangze/yourtv）。
