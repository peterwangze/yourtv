# 审计发现复核结果（F1-F23）

> 复核性质：只读核对（未修改任何源码/资源）
> 复核日期：2026-08-09
> 基线确认：git HEAD = `8185b72 v3.0.0`，`version.json` = version_code 300 / 3.0.0；工作区无已跟踪源码改动（仅 docs/、screenshots/ 未跟踪）
> 复核范围：`app/src/main/java/com/horsenma/yourtv/` 与 `app/src/main/res/`
> 结论总览：**F1-F23 全部仍成立（23/23），未发现已修复项，未发现与实际不符项**；F23 有一处措辞需要细化（见下）。

---

| 编号 | 结论 | 证据（文件:行号） | 一句话说明 |
|---|---|---|---|
| F1 | 仍成立 | ChannelFragment.kt:78；TVGroupModel.kt:247-255；MainActivity.kt:1320-1323、1100-1113 | `getCurrent()!!` 原样存在；`tvGroupValue.size < 3 \|\| tvGroupValue[1].size()==0` 时返回 null，数字键入口无"数据就绪/分组数≥3"守卫。 |
| F2 | 仍成立 | ErrorFragment.kt:19、23-30、32-33 | 第 19 行 inflate 并完成 px2Px 适配后，第 32 行再次 inflate 返回第二棵视图树，第一棵的缩放适配全部丢失，且每次重复 inflate。 |
| F3 | 仍成立 | MainActivity.kt:108-110、1233-1247、1325-1332、132-140、653-683 | MENU_PRESS_INTERVAL=300ms、REQUIRED_MENU_PRESSES=4；单按 MENU/SETTINGS 仅计数（1236/1243），无任何界面反应；触摸单击无操作、双击开菜单、≥4 连击开设置。 |
| F4 | 仍成立 | ListAdapter.kt:132-136；list_item.xml:47-48 | 频道行 DPAD_RIGHT 直接翻转 like 状态并 return true，无 toast/动画；heart 控件 clickable=true、focusable=false，用户无法聚焦感知。 |
| F5 | 仍成立 | ListAdapter.kt 全文（无 positionPlaying 引用）、GroupAdapter.kt 全文（同）；TVListModel.kt:48-57 | `positionPlayingValue` 数据存在但两个 adapter 均未消费，列表无"正在播放"角标/色条，初始焦点移走后无法找回当前频道。 |
| F6 | 仍成立 | ProgramAdapter.kt:118-135；program_item.xml:9；ProgramFragment.kt:56-77；program.xml；MainActivity.kt:1166-1171 | "现在"行仅文字变白；行背景固定 @color/blur；每次 onVisible 重建 adapter 并 `scrollToPositionWithOffset(index,0)` 顶对齐，index<0 不滚动；program.xml 无空态视图，EPG 为空仅在呼出前 toast 一次。 |
| F7 | 仍成立 | setting.xml:13-23、212-226、432-433；SettingFragment.kt:367-386、388-433 | 单列 330dp ScrollView 平铺 17 个 SwitchCompat + 6 个按钮（367-374），无分组标题；按钮/开关统一 textSizeSwitch（414）；switchEnableWebviewType 红色 18sp（setting.xml:215-216）。 |
| F8 | 仍成立 | SettingFragment.kt:240-247 | switchExit 勾选 isChecked=true 立即 `finishAffinity()`，无二次确认，与"清除设置"的 AlertDialog 确认不一致。 |
| F9 | 仍成立 | YourTVApplication.kt:73；SP.kt:211-213；SettingFragment.kt:167-173 | `onCreate` 无条件 `SP.compactMenu = true` 覆盖持久化值；设置页开关（167-173）改写的值在下次进程启动被重置。 |
| F10 | 仍成立 | ProgramFragment.kt:108-110；MainActivity.kt:1366-1413、124-130 | ProgramFragment.onKey 恒返回 false，ENTER 分支无 programFragment 短路，600ms 后 handleEnterRunnable 打开 menuFragment（对比 LEFT/RIGHT 分支 1453-1457 有短路）。 |
| F11 | 仍成立 | ListAdapter.kt:180-188；GroupAdapter.kt:134-142；ProgramAdapter.kt:118-135；SettingFragment.kt:378-385、416-432；button_scale.xml；item_background.xml；focus_background.xml:6；colors.xml:2 | 四套焦点表达并存：整行背景色（列表/分组）、仅文字变色（EPG/设置）；button_scale.xml（1.1x）与粉色 item_background.xml 全工程零引用；focus_background 仅 menu.xml:28/46/56 源切换器使用且描边 holo_blue_light 与应用青色 #0096A6 不一致。 |
| F12 | 仍成立 | InfoFragment.kt:117-122、90-114 | EPG 过滤仅 `beginTime < now` 后取 last()，未过滤 endTime（已结束且无后续节目时显示过期节目）；每次 show() 新建 300x180 ARGB_8888 Bitmap，无复用/回收。 |
| F13 | 仍成立 | MenuFragment.kt:82-86（group*2/3）与 227-239（group*4/5） | 首次创建分组列宽 groupWidth*2/3，设置页切换"紧凑菜单"后 updateSize 改为 groupWidth*4/5，布局跳动（list 两处均为 4/5，仅分组列不一致）。 |
| F14 | 仍成立 | MenuFragment.kt:120-136；menu.xml:21-22、88；GroupAdapter.kt:103-112 | 分组列表顶部 UP 对 GONE 的 source_switcher_prev requestFocus 并消费（120-129）；menu.xml 88 行 nextFocusUp 指向 GONE 控件；GroupAdapter 仅 DOWN 环绕（103-111），无 UP 环绕，行为不对称。 |
| F15 | 仍成立 | GroupAdapter.kt:177-181；MenuFragment.kt:216-225、273-280 | changed() 全量 notifyDataSetChanged；update() 每次 post 后 changed()+listAdapter.update；分组焦点每变化一次（273-280）即整组更新右侧列表。 |
| F16 | 仍成立 | loading.xml:1-21；LoadingFragment.kt:61-70 | 加载页纯黑底+ProgressBar 无文案；`removeFragment()` 与 `setCallback()` 全工程无调用点（rg 核实），callback 机制为死代码。 |
| F17 | 仍成立 | error.xml:1-32；ErrorFragment.kt（无按键/点击处理） | 错误页仅图标+单行 msg，无重试按钮，Fragment 无任何按键处理，用户只能盲按 BACK/方向键恢复。 |
| F18 | 仍成立 | MainActivity.kt:632-794；strings.xml（无手势文案） | 亮度/音量/滑动切台/双击菜单/长按 EPG 手势完整但零引导，无首次引导卡、无"手势说明"入口（全工程检索无相关字符串）。 |
| F19 | 仍成立 | MenuFragment.kt:827、854、875、882；MainActivity.kt:943-957；YourTVApplication.kt:145-162 | 切源/线路提示四处 Toast.makeText 直调 + showSourceInfo 自建 30f Toast 5s cancel，全部绕过全局 8s 同文案节流。 |
| F20 | 仍成立 | MainActivity.kt:541-552、558、576 | throttle() 内 observeForever 注册后无 removeObserver（全工程 rg 无 removeObserver）；每 TVModel 的节流观察者随 Activity 重建累积，存在重复触发风险。 |
| F21 | 仍成立 | ProgramFragment.kt:61-68 | onVisible() 每次新建 ProgramAdapter + LinearLayoutManager + setAdapter，滚动位置无法保持。 |
| F22 | 仍成立 | SourcesAdapter.kt:125；sources_item.xml:25-37 | 线路编号 `"%02d".format(position)` 从 00 起；title 固定 500dp 单行 ellipsize="start" 直接显示完整 URI，只露尾巴、无域名摘要。 |
| F23 | 仍成立（措辞细化） | MenuFragment.kt:594-891、689-690、916；onVisible:435；广播:171 | 切换器 UI 链路（updateSourceSwitcher/updateDisplaySource/switchSource/双击计数）在 SOURCE_FILE_SWITCHER_ENABLED=false（916）下全部隐藏（672-675 提前 return），688-690 重复调用 updateSourceText()。细化：setupSourceSwitcher 本体（594-665）仍被 onVisible（435）与 TEST_CODE_EXPIRED 广播（171）调用，承担缓存清理/索引重建职责，并非完全死代码——死的是切换器可见 UI 与 switchSource 链路，清理时建议保留该初始化逻辑或显式迁移。 |

---

## 复核说明

1. **基线一致**：git HEAD 与审计基线同为 v3.0.0（commit 8185b72），`version.json` 为 300/3.0.0，工作区无已跟踪源码改动，审计与复核针对同一代码快照。
2. **行号全部实测复核**：上表证据行号为本次逐行读取所得，与审计报告引用的行号一致（F2、F4、F9、F13、F19、F22、F23 等精确命中）。
3. **唯一修正项**：F23 的"约 300 行死代码"表述需按上文细化——`setupSourceSwitcher` 的初始化部分仍在运行，建议后续演进保留。
4. 本复核为静态代码级核对；F1 的崩溃触发、F3/F4 的按键行为等仍建议在真机回归中验证，结论以代码证据为准。
